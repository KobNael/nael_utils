.. _json_utils:

Json utilities
===================

One can easily import (resp. export) a data model from (resp. to) using the following methods,
provided that the context T is described using the macro MAKE_DTO_STRUCT (see :ref:`model_desc`).

.. doxygennamespace:: json
